<ul class="routes-list">
  <%= for {route_id, route_name} <- @sign_routes do %>
    <li>
      <%= link "#{route_name} signs", to: signs_path(@conn, :show, route: route_id) %>
    </li>
  <% end %>
  <li>
      <%= link "All signs", to: signs_path(@conn, :show, route: :all) %>
  </li>
</ul>
